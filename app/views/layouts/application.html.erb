<!DOCTYPE html>
<html>
  <head>
    <title>RubyTurbo</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if notice.present? %>
      <div class="py-2 px-3 bg-green-50 m-5 text-green-500 font-medium rounded-lg text-center" id="notice"><%= notice %></div>
    <% end %>
    <% if alert.present? %>
      <div class="py-2 px-3 bg-red-50 m-5 text-red-500 font-medium rounded-lg text-center" id="alert"><%= alert %></div>
    <% end %>
    <main class="container mx-auto mt-28 px-5 flex">
      <%= yield %>
    </main>
  </body>
</html>
